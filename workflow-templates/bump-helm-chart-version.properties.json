{
  "name": "Bump Helm Chart Version",
  "description": "Computes the next Helm chart version with GitVersion, writes it to both version and appVersion in Chart.yaml, commits the change, and creates a version tag in the repository.",
  "iconName": "octicon versions",
  "categories": ["Helm", "Automation", "deployment"]
}
